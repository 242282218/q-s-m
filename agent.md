# AGENT.md（精简版）

> 版本：4.1（2026-02-25）  
> 适用：Cursor / Trae / Copilot / Codex CLI / Gemini CLI  
> 输出：中文；代码标识符用英文

## 1. 硬性原则

1. 安全第一：禁止破坏性命令、泄露密钥、越权操作。  
2. 最小变更：仅修改任务相关内容，不做顺手优化。  
3. 先读后写：改前读上下文，改后立即校验。  
4. 可回滚：改动必须可由 `git revert` 撤销。  
5. 歧义先问：需求或风险不清时暂停并确认。

## 2. 执行边界

Always：说明改了什么与为什么；保持幂等；失败显式报错。  
Ask First：删文件/目录、超过 3 文件改动、依赖变更、CI/CD、Docker、数据库迁移、认证授权、外网请求。  
Never：输出密钥；执行 `git push --force`；无明确理由使用 `type: ignore` 或 `ts-ignore`。

## 3. 编码与提交流程

- Python：PEP8 + 类型注解。  
- JS/TS：ESLint + Prettier，优先 `const`。  
- 流程：理解需求 → 澄清疑点 → 实施 → 测试/构建/lint → 报告。  
- 提交：`<type>(<scope>): <中文描述>`，保持原子提交。

## 4. Skill 机制

- 不在规则里写死具体 Skill 名称。  
- 统一先调用 `find-skills` 自动发现可用 Skill，再按阶段选择。  
- 调用前确认输入/输出，调用后记录结果与影响。  
- 无可用 Skill 时回退手动方案，并说明原因。

## 5. 报告模板

目标｜变更文件与原因｜验证结果（构建/测试/Lint）｜风险（无则省略）
